import {StyledSnackbar} from './Snackbar.elements'

/**
 * Snackbar is a component that displays a snackbar message to the user to alert if he is not logged in.
 *
 * @return {ReactElement} The JSX markup for the Snackbar component
 */
const Snackbar = () => {
  return <StyledSnackbar>Vous devez choisir un utilisateur</StyledSnackbar>
}

export default Snackbar
